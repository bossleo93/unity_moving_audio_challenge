using UnityEngine;
using UnityEditor;

public class MoveObject : MonoBehaviour
{
    public float speed = 1.0f;

    void Update()
    {
        // Move the GameObject to the right over time
        transform.position += new Vector3(speed * Time.deltaTime, 0, 0);
    }
}
